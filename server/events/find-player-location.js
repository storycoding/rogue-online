const findPlayerLocation = (players, playerId) => {
    // the player's last position should be recorded in a database
    // the idea here is to fetch the player position by id

    return {
        r:5,
        c:9,
    }
}

module.exports = findPlayerLocation;
